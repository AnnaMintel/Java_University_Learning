package com.company;

// Во всех классах следует переопределить метод Equals, чтобы обеспечить сравнение значений

public class Test extends Money{
        public boolean equals()
        {
            return (first == second);
        }
    }

